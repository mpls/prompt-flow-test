def add(a: float, b: float) -> float:
    """
    Adds two numbers and returns the result as a float.

    Args:
        a: The first number.
        b: The second number.

    Returns:
        The sum of a and b.
    """
    return float(a + b)
